#include "Kinematics.h"

vector<StepPos> Kinematics::compressGridPath(vector<GridPos> gridPath)
{   
    
    return vector<StepPos>();
}

vector<StepPos> Kinematics::GridPathTOSteps(vector<GridPos> gridPath)
{
    vector<StepPos> stepsPath();

    return vector<StepPos>();
}